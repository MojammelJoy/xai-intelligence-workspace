import { WowReveal } from "./wow-reveal";

export function WowOutro() {
  return (
    <WowReveal className="mt-16 text-center">
      <p className="text-muted-foreground text-base sm:text-lg">
        From raw signal to automated action — Xai closes the loop.
      </p>
    </WowReveal>
  );
}
